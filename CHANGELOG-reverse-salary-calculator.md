# 목표 실수령액 역산 계산기 (2026-08, rev3 · 배포 승인본)

## 기능
원하는 월 실수령액 → 그 실수령을 받기 위한 "최소 세전 연봉"을 역산. 순방향
연봉계산기와 입력 계약·결과 카드를 공유하고 입력 방향만 뒤집는다.

## rev2에서 반영한 리뷰 (3)
1. [정확성] 옛 "원 단위 탐색 → 만원 올림" 설명 제거.
   - page.tsx 가이드/FAQ, ReverseCalculatorForm 결과 문구를 실제 동작(만원 그리드
     직접 탐색)과 일치하도록 수정. (코드-설명 불일치는 사용자에게 계산 방법을
     사실과 다르게 안내하는 오류이므로 최우선 수정.)
2. [입력 계약] 8~20세 자녀 수 입력 UI 추가.
   - childCount8to20을 폼에서 입력받아 엔진에 전달. 자녀 수 ≤ 부양가족 수 - 1 클램프.
   - ⚠️ 주의: 이 아카이브의 순방향 CalculatorForm에는 자녀 입력 UI가 없음.
     따라서 현재 역방향 폼이 순방향보다 입력이 더 많다. 진짜 동일 계약을 원하면
     순방향 폼에도 같은 자녀 UI를 추가해야 함(별도 결정 필요).
3. [알고리즘] 만원 그리드 전구간 단조성 전수 스캔 → 단조성 불성립 확인.
   - 240만 스텝 스캔에서 net 감소 지점 다수 발견(예: 부양 3명 7,008만원에서
     net -30,556원). 간이세액표 계단 함수로 인한 실제 비단조성.
   - 이분탐색(단조성 전제) 폐기 → 만원 그리드 선형 스캔으로 교체.
     0부터 올라가며 net >= target인 첫 연봉 반환 = 정의상 최소값(비단조성 무관 정확).
   - 검증: 목표=net(7008만,부양3명)일 때 6,959만원 반환(이분탐색이면 7,008만원으로
     49만원 과다 추천했을 값). 모든 표본에서 최소성·여유>=0 성립.


## rev3 최종 정리 (배포 전)
7. reverseSalary.ts 상단의 stale 주석(원 단위 최소값 → 만원 올림/ceil) 제거.
   실제 동작(만원 그리드 선형 스캔, 비단조 → 단조 전제 이분탐색 미사용)과 일치하도록 교체.
8. 입력 최대값과 검색 상한 정합: netOf(HI_BOUND=5억, 0/1/0)=24,358,406원 기준으로
   MAX_TARGET을 5,000만 → 2,400만원으로 조정(입력 허용 = 실제 도달 가능). 힌트 문구 동기화.

## 확정 사양 (유지)
- 별도 /target-salary-calculator 라우트, 월 목표 실수령 입력·연 실수령 병기
- 표시 연봉을 다시 calculateSalary()에 넣어 모든 결과 재계산(단일 출처)
- 결과: 필요 연봉 / 월 세전 / 예상 월 실수령 / 목표 대비 여유 / 4대보험·소득세 상세
- 보조 링크: 가장 가까운 /salary/[amount](핵심 CTA 아님)
- 보너스: "연봉 100만원 더 받으면 실수령 얼마나 느는가"(엔진 계산값, 숫자 먼저)

## sitemap
- /target-salary-calculator를 sitemap에 추가(routes.ts의 CALCULATOR_ROUTES).
  이 페이지는 27개 연봉 페이지와 달리 고유 검색 의도·기능이라 별개로 색인.
- /salary/[amount] 27개는 여전히 sitemap 제외(변경 없음).

## 변경/신규 파일 (5)
1. src/lib/reverseSalary.ts (신규)                          — 역산(선형 스캔)
2. src/lib/routes.ts (수정)                                 — 신규 라우트 등록 + sitemap 포함
3. src/components/calculator/ReverseCalculatorForm.tsx (신규) — 폼(자녀 UI 포함, 디자인/ResultCard 재사용)
4. src/app/target-salary-calculator/page.tsx (신규)         — 라우트 + 고유 가이드/FAQ(문구 수정)
5. src/app/salary-calculator/page.tsx (수정)               — 관련 계산기에 역산기 링크 1개

## 검증
- npx tsc --noEmit : 통과 / npm run build : 통과, 43페이지 생성
- 전구간 단조성 스캔(240만 스텝) / 선형 스캔 최소성·자녀효과·비단조구간 스크립트 검증
